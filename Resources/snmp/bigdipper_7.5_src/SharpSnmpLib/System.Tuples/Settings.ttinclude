<#+
	//change the value of this variable to
	//the length of the longest Tuple you need
	int maxTupleLength = 10;
	
	//should the generator add the
	//[DataContract] attribute to the classes?
	bool addDataContract = true;
	
	//should the generator create Tuple.ToList
	//and Tuple.ToTuple functions?
	bool generateConversions = true;
	
	//should the generator create extension methods
	//for ToList and ToTuple?
	bool generateExtension = true;
	
	//should the generator generate names like
	//First, Second .. for the elements
	//note: it does so only for the first maxOrdinal elements
	bool generateOrdinalNames = true;
	
	//if you change this be sure to update the
	//GetOrdinalNumber function in Util.ttinclude
	int maxOrdinal = 10;
	
	//should the generator make Head and Tail
	//properties in the Tuple<> classes
	bool generateHeadTail = true;
#>